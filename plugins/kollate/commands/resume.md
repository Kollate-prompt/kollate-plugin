---
description: Resume Kollate capture
---
Run `python3 ${CLAUDE_PLUGIN_ROOT}/hooks/kollate.py resume` and report its output in one line.

If `python3` is missing or is the Windows Store stub, run the same command with `python` instead.
