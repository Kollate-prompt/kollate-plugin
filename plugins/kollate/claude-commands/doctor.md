---
description: Diagnose where Kollate reads its credentials and hooks from on this machine
---
Run the plugin's `kollate.py doctor` and show the output verbatim as a code block. It prints no
secrets - only paths, booleans and timestamps - and is used to debug credential-location issues.
